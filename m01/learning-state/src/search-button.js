'use strict'

import React from 'react'
import Button from './button'

const SearchButton = () => {
  return (<Button handleClick={() => alert('search')}>Pesquisar</Button>)
}

export default SearchButton
